import React from "react";

function Instructions() {
    return (
        <div id="instructions">
            <h3>Try to match the Marvel characters, but you can click on each image only once!</h3>
        </div>
    );
}

export default Instructions;