import React from "react";

const NavBar = (props) => {
    return(
        <nav className="navbar navbar-dark bg-dark">
            <span className="navbar-brand ml-12" id="Nav">MARVEL Game</span>
        </nav>
    )
}

export default NavBar;
